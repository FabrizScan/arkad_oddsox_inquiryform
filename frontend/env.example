# Frontend Environment Variables Template
# COPIA questo file in .env.local e compila con i tuoi valori reali
# IMPORTANTE: .env.local NON deve essere committato nel git!

# Google Maps API Key (può essere pubblica, ma con restrizioni dominio)
VITE_GOOGLE_API_KEY=your-google-maps-api-key

# Backend API URL (per sviluppo locale)
VITE_BACKEND_API_URL=http://localhost:8787

# Note: 
# 1. Copia questo file in .env.local
# 2. Sostituisci i valori con le tue configurazioni reali
# 3. .env.local è già nel .gitignore e NON verrà committato
# 4. In produzione, queste variabili vanno configurate in Cloudflare Pages
# 5. VITE_BACKEND_API_URL in produzione sarà l'URL del tuo Cloudflare Workers
